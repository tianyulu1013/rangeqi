# Battle Array: Collapse — Collapse 演出与 Replay 工作流程

## 1. 目标

当前 Collapse 与 Replay 主要依靠 `marked / removed` 快照切换表现逐层消灭。玩家能够看到结算结果，但难以感受到“支撑结构断裂，引发下一层继续倒塌”的过程。

本工作流的目标是：

- 不改变任何棋局规则和 Collapse 计算结果。
- 将 Collapse 打造成游戏的核心视听卖点。
- 让玩家看懂每一层 Collapse 为什么发生。
- 让首次结算与 Replay 共用同一套演出系统。
- 保持抽象棋应有的清晰度，不使用喧宾夺主的爆炸和全屏特效。

---

## 2. 总体原则

Collapse 的重点不是“棋子消失”，而是下面这条因果链：

```text
棋子失稳
→ 棋子崩解
→ 攻击或支援关系断裂
→ 幸存棋子的生存值发生变化
→ 新棋子进入负生存状态
→ 触发下一层 Collapse
```

所有视觉、声音和节奏设计都应服务于这条因果链。

首次点击 `Settle` 时必须完整播放 Collapse 演出。Replay 的职责是重新播放同一段演出，而不是提供另一套简化版本。

---

## 3. 阶段一：拆分规则计算与演出状态

### 3.1 目标

Replay 不再只切换 `marked / removed` 两张棋盘快照。Collapse 规则层继续负责计算，表现层独立控制播放过程。

### 3.2 新增播放状态

```text
idle
→ announce
→ unstable
→ breaking
→ recalculate
→ nextRound
→ complete
```

各状态含义：

- `idle`：尚未开始播放。
- `announce`：宣布当前 Collapse 层级。
- `unstable`：展示本层将被消灭的棋子及其最低生存值。
- `breaking`：播放棋子崩解与退场动画。
- `recalculate`：更新攻击、支援和生存值，并显示受影响棋子。
- `nextRound`：短暂停顿，准备进入下一层。
- `complete`：棋盘稳定，播放结束。

### 3.3 每层需要保存的数据

- 消灭前的棋盘。
- 本层被消灭的棋子。
- 消灭后的棋盘。
- 消灭前后的攻击次数。
- 消灭前后的支援次数。
- 消灭前后的净生存值。
- 数值发生变化的幸存棋子。
- 当前轮数。
- 当前最低生存值。

### 3.4 实现约束

- Collapse 规则函数不得依赖动画计时器。
- 演出状态不得反向修改规则结果。
- 被消灭棋子的规则顺序保持不变。
- 同一轮的多枚棋子仍被视为同时消灭。
- Replay 快进、暂停、后退不得影响最终棋盘。

### 3.5 验收条件

- 首次结算与 Replay 生成完全相同的 Collapse 层级。
- Replay 可以安全地暂停、恢复、前进和后退。
- 无论动画是否播放完成，最终棋盘都与规则测试结果一致。

---

## 4. 阶段二：建立每层演出节奏

### 4.1 推荐时间线

每层控制在约 `1.3–1.5 秒`：

| 阶段 | 建议时长 | 表现内容 |
|---|---:|---|
| Announce | 180ms | 显示 `COLLAPSE I` 或 `CASCADE II` |
| Unstable | 320ms | 本层棋子变暗、负生存值亮起、轻微失稳 |
| Breaking | 280ms | 棋子裂开、收缩并消失 |
| Recalculate | 450ms | 幸存棋子的生存值变化，下一层目标出现 |
| Gap | 120ms | 给玩家辨认新局面的短暂停顿 |

### 4.2 同时死亡

- 同层多枚棋子可以相隔 `30–50ms` 开始动画。
- 所有同层棋子必须在同一 `breaking` 阶段内结束。
- 这种微小错峰只用于增强连片倒塌感，不能让玩家误认为棋子按顺序结算。

### 4.3 手动与自动播放

- 自动播放执行一层内的完整五段节奏。
- “下一步”按一整层推进，不要求玩家逐个点击子阶段。
- “上一步”回到上一层开始前的稳定快照。
- 暂停应冻结动画、计时器和声音。

### 4.4 验收条件

- 玩家可以清楚区分“判定”“崩解”和“重算”。
- 层与层之间不存在无意义的长等待。
- 自动播放不会快到无法理解因果关系。

---

## 5. 阶段三：制作棋子崩解动画

### 5.1 视觉方向

使用“结构断裂”而不是“爆炸”：

1. 棋子出现一条明显裂纹。
2. 棋子的两部分产生轻微错位。
3. 棋子向中心收缩并略微下沉。
4. 棋子颜色迅速褪去。
5. 格子内留下一个短暂尘环，然后消失。

### 5.2 技术要求

- 进入 `breaking` 后，不能立即从页面中删除棋子。
- 为本层被消灭的棋子保留临时动画副本。
- 可以使用两个裁切后的棋子图层表现裂开。
- 碎片和尘环必须限制在棋子所在格内。
- 棋盘冲击最多产生 `1–2px` 的轻微位移。
- 不做全屏震动、强闪白或大范围粒子喷射。

### 5.3 不同规模的处理

- 单枚死亡：完整展示裂开、收缩和下沉。
- 多枚死亡：使用极短错峰形成连片崩塌感。
- 两个阵营同时死亡：保持各自阵营色，不改变规则上的同时性。

### 5.4 验收条件

- 棋子不会突然从棋盘上闪没。
- 玩家能够明确看出是棋子自身结构崩坏。
- 多枚棋子同时崩解时，手机仍能保持流畅。

---

## 6. 阶段四：表现连锁原因

### 6.1 目标

让玩家理解：上一层棋子消失后，为什么下一层棋子会死亡。

### 6.2 数值变化

棋子消失后，比较消灭前后的状态，为受影响的幸存棋子生成变化数据：

```text
Support 2 → 1
Attack 2 → 1
Net Survival 0 → -1
```

画面中只显示简短变化：

- `−1 Support`
- `−1 Attack`
- `Survival 0 → −1`

### 6.3 表现规则

- 失去支援导致恶化：使用明确的警告色。
- 敌方攻击消失导致改善：使用恢复色。
- 新变成负数的棋子明显闪动一次。
- 新进入负数状态的棋子随后成为下一层的 Collapse 目标。
- 不在棋盘底部增加大段解释列表。

### 6.4 可选的关系断裂线

- 从刚消失的棋子位置连接到受到影响的棋子。
- 线条短暂断开并迅速消失。
- 只显示数值确实发生变化的关系。
- 不显示完整攻击与支援网络。

### 6.5 验收条件

- 玩家能通过棋盘动画理解第二层 Collapse 的来源。
- 数值提示不会遮挡棋子图标。
- 大量关系同时变化时，系统会省略次要提示，保证清晰度。

---

## 7. 阶段五：声音系统

### 7.1 基础声音

准备四类本地或程序化声音：

1. `mark`：短促、偏高的紧张音，用于本层判定。
2. `crack`：清晰的木材或石块断裂声。
3. `impact`：很轻的低频下沉声。
4. `stable`：结算完成后的短收束音。

### 7.2 连锁层级变化

- 第一层使用正常音高。
- 后续每层略微降低音高。
- 后续每层的低频和重量小幅增加。
- 深层 Collapse 不得突然大幅提高音量。

### 7.3 同时崩解

- 每层 `impact` 只播放一次。
- 每枚棋子的 `crack` 可以错开约 `35ms`。
- 同时死亡数量较多时限制叠加声音的数量。
- 声音错峰不得让玩家误认为棋子按顺序死亡。

### 7.4 浏览器与手机要求

- 在玩家点击 `Settle` 或 `Play` 时初始化音频上下文。
- 设置中增加 `Sound On / Off`。
- 静音设置写入本地存储。
- 页面进入后台或暂停 Replay 时立即停止未完成的声音。
- 手机版可以增加一次很轻的震动，但震动必须可以关闭。

### 7.5 验收条件

- iOS、Android 和微信内置浏览器不会因自动播放限制导致报错。
- 静音后不再创建新的可听声音。
- 刷新页面后保留声音设置。
- 暂停或退出 Replay 后没有残留声音。

---

## 8. 阶段六：强化层级标题

### 8.1 英文标题

```text
COLLAPSE I
CASCADE II
CASCADE III
FORMATION STABLE
```

### 8.2 中文预留

```text
崩解 I
连锁 II
连锁 III
阵型稳定
```

### 8.3 显示要求

- 标题短暂浮在棋盘中央。
- 标题显示时间不超过约 `400ms`。
- 标题不得长期遮挡棋盘。
- 层数越深可以略微增强冲击力，但字体不持续放大。
- 结果页可以显示本局的总连锁层数，例如 `3-LAYER COLLAPSE`。

### 8.4 验收条件

- 手机竖屏上标题左右完整可见。
- 标题不会改变棋盘尺寸或引发页面跳动。
- 玩家可以立即辨认当前处于第几层 Collapse。

---

## 9. 阶段七：统一首次结算与 Replay

### 9.1 首次结算流程

```text
完整阵型
→ 玩家点击 Settle
→ 自动播放 Collapse
→ Formation Stable
→ 显示比赛结果
```

### 9.2 Replay 流程

```text
回到完整阵型
→ 使用同一个播放控制器重新演出
→ 允许暂停、上一层、下一层和重新播放
```

### 9.3 禁止出现的情况

- 首次结算有动画，但 Replay 没有。
- Replay 与首次结算使用不同节奏。
- 手动前进时棋子突然闪现或消失。
- 暂停后声音仍继续播放。
- 退出 Replay 后仍残留动画图层。

### 9.4 验收条件

- 两种入口共用同一播放控制器。
- 同一个 Collapse 层在首次结算和 Replay 中表现一致。
- 退出 Replay 后能够正确恢复最终棋盘。

---

## 10. 阶段八：移动端、性能与无障碍

### 10.1 移动端

- 所有效果限制在棋盘区域内。
- 动画不能扩大页面宽度或产生横向滚动。
- 低性能设备限制同时存在的碎片数量。
- 页面进入后台时暂停播放。
- 声音开关同时放在 Replay 控制区和设置页。

### 10.2 Reduced Motion

检测 `prefers-reduced-motion`：

- 取消抖动。
- 取消碎片位移。
- 保留颜色变化、淡出、层级标题和数值变化。
- 规则信息不能依赖声音或动画才能理解。

### 10.3 性能目标

- 手机竖屏播放过程中维持稳定帧率。
- 不为每个碎片创建长期存在的页面节点。
- 每轮结束后清理动画计时器、临时棋子和音频节点。

---

## 11. 固定测试场景

至少建立以下测试局面：

1. 没有棋子死亡，直接稳定。
2. 单层只死亡一枚棋子。
3. 单层同时死亡多枚棋子。
4. 红蓝棋子在同一层同时死亡。
5. 两层连锁。
6. 三层以上深度连锁。
7. 敌方攻击者消失后，幸存棋子的生存值改善。
8. 友方支援者消失后，新棋子进入负生存状态。
9. 自动播放中暂停、继续和退出。
10. 手动执行上一层和下一层。
11. 静音状态刷新后仍然保留。
12. 页面进入后台再返回。
13. `prefers-reduced-motion` 模式。
14. 微信内置浏览器中的声音解锁和播放。

---

## 12. 推荐实施顺序

### 第一批：建立核心 Collapse 体验

1. 新建 Collapse 播放状态机。
2. 扩充每层的前后状态与数值变化数据。
3. 建立被移除棋子的临时动画层。
4. 完成裂开、收缩、下沉和消失动画。
5. 加入 `mark / crack / impact / stable` 四类声音。
6. 让首次结算和 Replay 共用播放控制器。

完成标准：Collapse 不再是快照切换，而是一段完整、可重播的逐层崩解演出。

### 第二批：强化因果表达

1. 加入攻击、支援和生存值变化动画。
2. 加入新一层目标出现的视觉反馈。
3. 视清晰度决定是否加入关系断裂线。
4. 加入 `COLLAPSE / CASCADE / STABLE` 层级标题。
5. 在结果页面显示总连锁层数。

完成标准：玩家无需阅读底部说明，也能理解连锁崩溃为何继续。

### 第三批：移动端打磨

1. 增加声音与震动设置。
2. 适配微信、iOS 和 Android 的音频限制。
3. 加入 Reduced Motion。
4. 限制低性能设备的碎片和音频数量。
5. 完成所有固定场景的视觉与声音验收。

完成标准：手机竖屏下演出稳定、清晰，并且不会阻塞操作。

---

## 13. 最终完成定义

只有同时满足以下条件，Collapse 演出才算完成：

- 首次结算和 Replay 使用同一套演出。
- 玩家能够听见并看见每层结构崩解。
- 玩家能够理解上一层如何触发下一层。
- 同时死亡不会被误解为依次死亡。
- 声音可以关闭并正确记忆设置。
- 手机和微信内置浏览器能够正常播放。
- Reduced Motion 下仍能完整理解结算。
- 动画不改变任何规则结果。
- `Battle Array: Collapse` 中的 “Collapse” 成为可以被玩家记住的核心体验。
