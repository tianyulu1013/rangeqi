# Battle Array: Collapse — 手机竖屏 UI 执行流程

## 1. 本流程的目标

把当前以桌面三栏布局为主的单页面游戏，重构成适合手机竖屏发布的三个顶层界面：

```text
起始界面
├─ Classic 界面
└─ Arena 界面
```

本流程只改变信息架构、页面布局、导航和移动端交互，不新增棋子、不调整棋子强度、不重做 AI 策略，也不改变 Classic 与 Collapse 的规则。

完成本流程之后，再执行 `PIECE_CONTENT_WORKFLOW.md`。

---

## 2. 不可破坏的现有功能

开始前记录并确认以下功能正常：

- Classic 为固定 7×7 棋盘和固定双方阵容。
- Arena 已有确定性种子、Fence、Draft 和动态阵容骨架。
- 手牌默认不选择棋子。
- 选择手牌后，点击合法棋盘格立即落子。
- 点击棋盘棋子可以查看“谁影响它 / 它影响谁”。
- 关系高亮使用红蓝阵营颜色，攻击与支援使用文字区分。
- Collapse、结果页面和清算复盘可用。
- 英语为默认语言。
- 图标为默认棋子显示方式。
- 英文标题为 `Battle Array: Collapse`。
- 中文标题保持“阵衡”。

执行者不得用重置工作区的方式解决冲突，不得覆盖用户现有未提交改动。

---

## 3. 最终信息架构

### 3.1 顶层界面

```ts
type AppScreen = "home" | "classic" | "arena";
```

不要继续使用游戏顶部的 Classic / Arena 即时切换器。模式切换只能通过起始界面完成。

### 3.2 共享对局阶段

```ts
type BattlePhase =
  | "placement"
  | "ready"
  | "settling"
  | "finished";
```

### 3.3 Arena 内部阶段

```ts
type ArenaStage =
  | "battlefield"
  | "core-draft"
  | "advanced-draft"
  | "battle";
```

UI 流程阶段可以先准备 `core-draft` 和 `advanced-draft` 两个界面状态，但本流程不负责增加新棋或决定最终棋池。候选数据必须通过参数传入，禁止在组件里写死棋种。

### 3.4 页面关系

```text
HomeScreen
├─ ClassicScreen
│  └─ Placement → Ready → Collapse → Result
└─ ArenaScreen
   └─ Battlefield → Core Draft → Advanced Draft
      → Placement → Ready → Collapse → Result
```

---

## 4. 阶段零：保护现状并建立 UI 基线

### 任务

- [ ] 运行 `git status --short` 并保存当前文件列表。
- [ ] 不删除、覆盖或回退现有改动。
- [ ] 运行现有 lint、规则测试、应用测试和静态构建。
- [ ] 分别记录桌面宽屏、手机竖屏和手机横屏的当前截图。
- [ ] 记录以下宽度的界面状态：320、360、390、430、768、1024、1440 像素。
- [ ] 记录当前最容易溢出或遮挡的区域。

### 验收标准

- 改造前状态可对比、可复现。
- 当前测试全部通过，或者已明确记录与 UI 改造无关的已有失败。

---

## 5. 阶段一：拆分页面壳与共享组件

### 目标

先拆组件，再改布局。不要继续把所有页面状态和 JSX 堆入 `app/page.tsx`。

### 建议结构

```text
components/game/
├─ HomeScreen.tsx
├─ ClassicScreen.tsx
├─ ArenaScreen.tsx
├─ BattleScreen.tsx
├─ BattleBoard.tsx
├─ PlayerHand.tsx
├─ OpponentSummary.tsx
├─ TurnHeader.tsx
├─ BattleActions.tsx
├─ PieceInspectorSheet.tsx
├─ SettingsSheet.tsx
├─ RulebookSheet.tsx
├─ BattleLogSheet.tsx
├─ ResultSheet.tsx
└─ ConfirmDialog.tsx

components/arena/
├─ BattlefieldStage.tsx
├─ BattlefieldPreview.tsx
├─ DraftStage.tsx
├─ DraftOfferCard.tsx
└─ RosterSummary.tsx
```

实际文件名可以调整，但必须满足：

- Classic 和 Arena 共用同一个棋盘组件。
- Classic 和 Arena 共用同一套手牌、棋子查看、Collapse 和结果组件。
- 设置、规则书和战局记录使用共享抽屉组件。
- 页面组件不重新实现规则计算。
- Arena 可以增加阶段，但不能复制一套 BattleScreen。

### 验收标准

- 玩家可见行为暂时不变。
- `app/page.tsx` 主要负责顶层状态与页面切换。
- 桌面和手机仍可完成 Classic 对局。
- 所有现有测试通过。

---

## 6. 阶段二：制作起始界面

### 目标

把模式选择从游戏顶部移到独立起始页。

### 页面内容

```text
Battle Array: Collapse

[ CLASSIC ]
Same armies. Same battlefield.
Pure formation strategy.

[ ARENA ]
Draft your army for a different battlefield every match.

[ Settings ]    [ Rulebook ]
```

中文：

```text
阵衡

[ CLASSIC ]
相同军队，相同战场，纯粹比较布阵。

[ ARENA ]
面对每局不同的战场，临场构筑你的军队。

[ 设置 ]    [ 规则书 ]
```

### 任务

- [ ] 删除正式游戏顶部的 Classic / Arena 切换器。
- [ ] 制作两个明显但不过度复杂的模式入口卡片。
- [ ] Classic 和 Arena 卡片在手机上纵向排列。
- [ ] 桌面上可以并排排列，但顺序始终为 Classic 在前。
- [ ] 默认聚焦 Classic，但不自动进入任何模式。
- [ ] 将语言、规则书和设置放在次级位置。
- [ ] 不在首页展示 AI 风格、先手、主题等全部选项。
- [ ] 点击模式后才打开该模式的轻量准备步骤，或直接使用现有设置进入。

### 导航规则

- 首次打开进入起始界面。
- 从模式返回起始界面时，如果尚未发生落子，可以直接返回。
- 已经开始 Draft 或落子时，返回必须要求确认。
- 取消确认后必须保持当前对局完整。
- 返回起始界面不应在确认前提前重置状态。

### 验收标准

- 首页只有 Classic 和 Arena 两个主要入口。
- 玩家不会把 AI / 本地双人误认为第三种核心规则模式。
- 手机上的首页无需滚动即可看到两个模式入口。

---

## 7. 阶段三：重排 Classic 竖屏界面

### 目标

Classic 在竖屏手机中始终保持棋盘为视觉主体，手牌和操作不能遮挡棋盘。

### 推荐布局

```text
┌────────────────────────┐
│ ← Classic   回合状态  ⚙ │
├────────────────────────┤
│ 对手阵容：图标 × 数量   │
├────────────────────────┤
│                        │
│        7×7 棋盘         │
│                        │
├────────────────────────┤
│ 我的手牌：聚合棋种按钮  │
├────────────────────────┤
│ 撤回             清算   │
└────────────────────────┘
```

### 顶部栏

- [ ] 高度控制在约 44～52 CSS px。
- [ ] 左侧为返回起始页。
- [ ] 中间显示当前阶段或当前行动方。
- [ ] 右侧只保留设置图标。
- [ ] 对局中不重复显示完整游戏标题。
- [ ] 不在顶部常驻语言下拉框、模式切换、规则书文字按钮和重开按钮。

### 对手摘要

- [ ] 使用棋子图标和剩余数量。
- [ ] 相同棋种聚合显示。
- [ ] 高度尽量控制在一行，空间不足时最多两行。
- [ ] 点击可打开完整对手阵容抽屉。

### 棋盘

- [ ] 棋盘保持正方形。
- [ ] 手机宽度优先，建议最大宽度为 `calc(100vw - 16px)`。
- [ ] 使用 `100dvh` 而不是传统 `100vh` 处理移动浏览器地址栏。
- [ ] 使用 `env(safe-area-inset-*)` 处理刘海和底部手势区。
- [ ] 手牌展开、设置打开或文字变长时不得把棋盘推到不可见区域。
- [ ] 320 像素宽度下棋子、生存值和关系标签仍可辨认。

### 手牌

- [ ] 相同棋种按一张按钮聚合，例如 `×3`。
- [ ] 默认不选中任何棋子。
- [ ] 选择后有明确但不夸张的高亮。
- [ ] 落子后立即清除选择。
- [ ] 每行最多 4 个紧凑棋种按钮，优先使用两行布局。
- [ ] 棋种数量未来增加时，手牌区域内部可以滚动，但不能推动或覆盖棋盘。
- [ ] 不显示对手机没有帮助的长篇棋子说明；长按或信息按钮再查看详情。

### 底部操作

- [ ] 只显示当前阶段真正可用的操作。
- [ ] 布阵阶段显示撤回。
- [ ] 20 枚棋落完后显示清算主按钮。
- [ ] 清算阶段显示必要的复盘或状态控制。
- [ ] 使用底部安全区间距，避免系统手势区域遮挡。

### 验收标准

- 320×568 与 390×844 均能完成整局。
- 棋盘不被手牌遮挡。
- 所有主要落子操作单手可完成。
- Classic 不出现任何 Arena 专属信息。

---

## 8. 阶段四：制作 Arena 独立流程

### 目标

Arena 不再作为 Classic 页面中的大型占位区域，而是拥有自己的连续阶段。

### 8.1 战场阶段

页面只展示本局出题信息：

```text
← Arena       Battlefield       ⚙

战场预览
[ 7×7 Fence 地图 ]

Seed: A72F31                     ⋯

[ Begin Draft ]
```

任务：

- [ ] 默认只显示短种子。
- [ ] “复制种子、输入种子、新战场”放入种子菜单或底部抽屉。
- [ ] Fence 图例保持简短。
- [ ] 战场预览尽量大，但必须为后续 Draft 保留独立页面。
- [ ] 点击 Begin Draft 后锁定本局地图，不得无提示更换。

### 8.2 基础棋 Draft 阶段

本阶段先完成通用 UI，候选内容由棋池流程接入。

```text
← Arena       CORE 3/7          ⚙

[ 战场缩略图 · 点击查看 ]

[ 候选卡一：横向 ]
[ 候选卡二：横向 ]
[ 候选卡三：横向 ]

我的阵容 3/10
[聚合图标]
```

### 8.3 高级棋 Draft 阶段

基础 Draft 完成后显示短转场：

```text
Core formation complete
Choose three advanced pieces.
```

然后进入：

```text
ADVANCED 1/3
```

基础和高级不得在同一轮候选中混合。

### 8.4 Draft 卡片

手机使用三张纵向横卡，不使用三列窄卡。

每张卡包括：

- 棋子图标。
- 棋子名称。
- `CORE / 基础` 或 `ADVANCED / 高级` 标签。
- 小型范围图。
- 一句话能力说明。
- 当前已选数量。
- 整张卡可点击。

卡片组件必须从候选数据读取内容，不得根据具体棋名写条件布局。

### 8.5 Draft 完成

- [ ] 显示双方最终阵容摘要。
- [ ] 相同棋种按数量聚合。
- [ ] 玩家点击“开始布阵”后才进入棋盘。
- [ ] 不要在第十次选择后毫无提示地瞬间跳到棋盘。

### 8.6 Arena 布阵

复用 Classic 的 BattleScreen，只增加：

- Fence。
- 当前种子的简短入口。
- Draft 产生的动态双方阵容。
- Arena 模式名称。

不得增加第二套棋盘和第二套 Collapse UI。

### 验收标准

- 玩家始终知道自己位于战场、基础 Draft、高级 Draft 或布阵中的哪个阶段。
- 手机 Draft 三张候选无需横向滚动。
- Draft 页面不会同时挤入完整地图、种子输入框、双方完整阵容和大量说明。
- Arena 布阵与 Classic 的核心操作位置一致。

---

## 9. 阶段五：把辅助内容改为底部抽屉

### 目标

对局主界面只保留高频操作，低频内容使用 Bottom Sheet。

### 设置抽屉

包含：

- 语言。
- 棋子显示方式。
- 颜色主题。
- AI 风格。
- 先后手设置，仅在新对局前允许改变或明确提示会重开。
- 重新开始。
- 返回模式选择。

要求：

- “重新开始”和“返回模式选择”位于独立的底部危险操作区。
- 打开和关闭设置不得改变游戏状态。
- 修改纯视觉设置不得重开对局。

### 规则书抽屉

- 根据当前模式首先显示相关内容。
- Classic 不强迫玩家阅读 Fence 与 Draft。
- Arena 显示随机战场、Fence、基础 Draft 和高级 Draft。
- 仍可切换查看完整通用规则。

### 棋子查看抽屉

- 点击棋盘棋子后，从底部显示紧凑信息。
- 保留“谁影响它 / 它影响谁”。
- 抽屉不能遮住被查看棋子；必要时只显示半高状态。
- 再次点击棋盘退出关系视角并关闭抽屉。

### 战局记录抽屉

- 不再常驻占用主界面高度。
- 从回合状态或独立小图标进入。
- 默认显示最近记录，允许向上查看更多。

### 结果与清算复盘

- 结果使用底部大抽屉或全屏轻页面。
- 复盘进入后仍以棋盘为主体。
- 上一步、播放、下一步、退出在手机上保持一行或两行稳定布局。

### 验收标准

- 主对局页面没有常驻的大块说明卡和日志卡。
- 辅助信息可找到，但不抢占棋盘空间。
- 所有抽屉支持关闭、返回键和焦点管理。

---

## 10. 阶段六：竖屏锁定与横屏处理

### Android

- [ ] 在 Android Activity 配置中设置 portrait 方向。
- [ ] 验证旋转设备时 Activity 不会重建或丢失对局。
- [ ] 验证状态栏、导航栏和全面屏安全区。
- [ ] 验证键盘弹出输入种子时不会破坏页面。

推荐结果是 Android 安装包真正锁定竖屏。

### 手机网页

普通浏览器不能可靠强制方向锁定，因此采用：

- 竖屏为完整交互界面。
- 触屏设备横屏时显示“请旋转至竖屏”的轻提示。
- 提示不得错误覆盖桌面宽屏浏览器。
- 判断时结合方向、视口尺寸和 coarse pointer，不只使用宽高比。
- 必须保留退出或继续使用的可访问方式，避免浏览器特殊情况下完全卡死。

### 桌面

- 桌面继续支持宽屏。
- 可以在较宽尺寸下把对手摘要、棋盘和手牌适度展开。
- 桌面布局只是竖屏结构的增强版，不恢复三栏信息堆叠。

### 验收标准

- Android 始终竖屏。
- 手机浏览器横屏提示正确。
- 桌面浏览器不会被误判为需要旋转。

---

## 11. 阶段七：视觉与交互统一

### 任务

- [ ] 统一顶部栏高度、页面左右边距和底部安全区。
- [ ] 统一所有 Bottom Sheet 的圆角、遮罩、拖拽条和关闭方式。
- [ ] 统一主按钮、次按钮和危险按钮。
- [ ] 统一 Core 与 Advanced 标签样式，但本阶段不决定具体棋子归属。
- [ ] 保持红蓝只表达阵营，不用红蓝表达按钮状态或棋子等级。
- [ ] 攻击和支援继续靠文字表达，不建立与阵营冲突的新颜色规则。
- [ ] Fence 使用中性色。
- [ ] 所有触摸目标至少约 44×44 CSS px。
- [ ] 动画尊重 `prefers-reduced-motion`。
- [ ] 所有图标按钮都有中英文辅助标签。

### 验收标准

- 玩家不会混淆阵营色、棋子等级和攻防状态。
- 320 像素宽度下没有难以点击的小按钮。
- 中英文切换不会造成关键布局错位。

---

## 12. 阶段八：完整验证

### 自动检查

- [ ] ESLint 通过。
- [ ] 规则单元测试通过。
- [ ] 应用构建和现有测试通过。
- [ ] 静态网页构建通过。
- [ ] Android 同步通过。

### 手机尺寸

至少验证：

- [ ] 320×568。
- [ ] 360×800。
- [ ] 390×844。
- [ ] 412×915。
- [ ] 430×932。

### 对局路径

- [ ] 起始页 → Classic → 完整对局 → 结果 → 再来一局。
- [ ] 起始页 → Arena → 战场 → 基础 Draft → 高级 Draft → 完整对局。
- [ ] 对局中打开并关闭设置、规则书、棋子查看和日志。
- [ ] 有进度时尝试返回首页，确认提示和取消行为。
- [ ] 中文完整路径。
- [ ] 英文完整路径。
- [ ] Android 真机竖屏完整路径。

### 最终验收标准

- 起始、Classic、Arena 是三个清晰界面。
- Classic 页面不显示 Arena 专属内容。
- Arena 的四个内部阶段顺序明确。
- 棋盘在竖屏中始终是视觉主体。
- 设置和辅助信息不再挤占主界面。
- UI 重构没有改变任何棋子与 Collapse 规则。

---

## 13. 建议提交顺序

1. `refactor: split top-level game screens`
2. `feat: add dedicated home mode selection`
3. `refactor: extract shared battle components`
4. `feat: rebuild classic portrait layout`
5. `feat: add staged arena mobile flow`
6. `feat: move settings and game info into sheets`
7. `feat: lock android to portrait orientation`
8. `fix: handle mobile web landscape and safe areas`
9. `test: cover navigation and responsive game flows`

每次提交只包含一个阶段。未经用户明确要求，不得自动推送。

---

## 14. Luna 第一轮执行范围

第一轮只完成：

1. 保护现有工作区。
2. 拆分顶层页面和共享 BattleScreen。
3. 制作独立起始界面。
4. 移除游戏顶部的规则模式切换器。
5. 建立返回起始页与未保存进度确认。
6. 保持 Classic 与 Arena 当前功能不变。
7. 运行测试并汇报视觉差异、修改文件和遗留风险。

第一轮验收后，再开始 Classic 竖屏排布；不要第一轮同时改 Draft、Bottom Sheet 和 Android 方向锁定。
