# Battle Array: Collapse — 棋子与棋池丰富执行流程

## 1. 本流程的开始条件

本流程必须在 `MOBILE_UI_WORKFLOW.md` 完成后开始。

开始前必须确认：

- 起始界面、Classic 界面和 Skirmish 界面已经分离。
- Skirmish 已有独立的战场、基础 Draft、高级 Draft 和布阵阶段。
- Draft 卡片与手牌组件不再写死现有六种棋。
- 手机竖屏可以容纳三张候选卡和动态阵容。
- Classic 使用独立固定阵容配置。
- 新增棋种不会自动进入 Classic。

如果以上条件未满足，应返回 UI 流程，不要一边增加棋子一边反复推翻页面结构。

---

## 2. 内容目标

Skirmish 中建立两个彼此独立的棋池：

```text
基础棋池 Core
└─ 每方选择 7 枚

高级棋池 Advanced
└─ 每方选择 3 枚
```

双方最终阵容：

```text
7 枚基础棋 + 3 枚高级棋 = 10 枚棋
```

基础棋和高级棋不在同一轮候选中混合。

---

## 3. 棋子等级定义

### 基础棋 Core

基础不等于弱。基础棋负责组成阵型主体，应满足大部分条件：

- 控制规则一句话可以讲清楚。
- 控制范围稳定。
- 通常控制约 4 个格子。
- 不依赖复杂的场上条件。
- 可以重复选择并大量进入阵容。
- 多枚同类棋仍能产生有意义的阵型。
- 新玩家看到范围图即可理解。

当前基础棋：

- 剑兵 `guard`：正交相邻 4 格。
- 枪兵 `scout`：斜向相邻 4 格。
- 射手 `archer`：正交距离 2 的 4 格。

### 高级棋 Advanced

高级棋是阵容的关键部件，应满足至少一个条件：

- 控制格明显更多。
- 使用射线、跳跃或阻挡条件。
- 与 Fence 或其他棋子发生特殊互动。
- 位置正确时具有较高上限。
- 不适合无脑堆叠。
- 需要玩家理解额外规则。

当前高级棋：

- 堡垒 `fortress`：控制周围 8 格。
- 骑士 `knight`：控制最多 8 个跳跃格，并可跨越 Fence。
- 炮台 `cannon`：依靠隔子启动直线控制。

堡垒不得归入基础棋。它具有稳定的 8 格邻接控制，是当前最直接的高级范围棋之一。

---

## 4. 第一阶段：建立可扩展棋子注册表

### 目标

新增棋子不再要求在大量 UI 和规则文件中分别添加硬编码判断。

### 建议数据结构

```ts
type PieceTier = "core" | "advanced";

type PieceTag =
  | "adjacent"
  | "ranged"
  | "jump"
  | "line"
  | "formation"
  | "terrain";

type PieceDefinition = {
  type: PieceType;
  tier: PieceTier;
  tags: PieceTag[];
  classicEnabled: boolean;
  skirmishEnabled: boolean;
  draftWeight: number;
  maxDraftAppearances: number;
  maxRosterCopies: number;
  names: { zh: string; en: string };
  descriptions: { zh: string; en: string };
  rangeSummary: { zh: string; en: string };
};
```

能力计算可以使用能力类型与参数，或指向集中管理的纯函数。不得把函数对象序列化进存档。

### Classic 固定阵容

Classic 必须显式配置：

```ts
const CLASSIC_ROSTER = {
  guard: 3,
  scout: 3,
  archer: 1,
  cannon: 1,
  knight: 1,
  fortress: 1,
};
```

新增棋子默认：

```ts
classicEnabled: false
skirmishEnabled: true
```

不得继续通过“所有已注册棋子的默认数量总和”计算 Classic 阵容。

### 验收标准

- 新棋注册后不会进入 Classic。
- UI 可以从注册表生成名称、说明、等级标签和范围资料。
- Draft 可以直接按 tier 取得候选池。
- 规则书可以按基础/高级自动分组。

---

## 5. 第二阶段：确定首批棋池规模

### 推荐首发目标

```text
基础棋：现有 3 + 新增 3 = 6 种
高级棋：现有 3 + 新增 3 = 6 种
```

六种不是强制数字，但每个池正式随机三选一时最好至少有 5 种。只有 3 种时，每次候选都会完全相同；只有 4 种时，变化仍然有限。

### 任务

- [ ] 先提出至少 6 个基础棋候选概念。
- [ ] 先提出至少 6 个高级棋候选概念。
- [ ] 只选出其中各 3 个进入第一批实现。
- [ ] 每个候选必须先写纸面规则，不直接写代码。
- [ ] 明确它与现有棋子的差异。
- [ ] 明确它在 Fence 地图中的行为。
- [ ] 明确它的传入/传出关系应如何显示。
- [ ] 明确它为什么属于基础或高级。

### 每枚候选棋的设计卡

```text
中文名：
英文名：
等级：Core / Advanced
一句话能力：
控制格示意：
遇到棋盘边缘：
遇到 Fence：
遇到其他棋子：
能否跨越阻挡：
典型控制格数量：
最大控制格数量：
适合支援还是攻击：
与哪种现有棋形成组合：
可能被滥用的方式：
```

### 验收标准

- 用户明确批准首批新增名单后才进入编码。
- 新棋不是仅仅把旧棋范围扩大一格。
- 基础棋仍然容易理解。
- 高级棋有特殊价值，但不是基础棋的无条件升级版。

---

## 6. 第三阶段：实现基础 Draft 与高级 Draft

### 最终 Draft 结构

```text
Core Draft
└─ 7 轮，每轮从基础池给出 3 个不同候选

Advanced Draft
└─ 3 轮，每轮从高级池给出 3 个不同候选
```

### 通用规则

- 双方每轮看到相同三个候选。
- 双方允许选择同一个候选。
- 候选不会因为一方选择而被另一方拿走。
- 每轮三个候选属于同一个 tier。
- 基础和高级候选不得混在同一轮。
- 候选全部由种子预先生成。
- 候选不得根据玩家已经选择的内容暗中改变。

### 基础棋限制

- 每种基础棋在单方阵容中建议最多 3 枚。
- 同一基础棋在整局候选中不得出现得过度频繁。
- 生成器必须保证玩家每轮至少有一个合法选择。

### 高级棋限制

- 每种高级棋在单方阵容中默认最多 1 枚。
- 若某种高级棋经过验证适合重复，可单独配置为 2 枚。
- 第三轮高级候选必须保证双方都有合法选择。
- 已达个人上限的候选对该玩家禁用，但不影响对方选择。

### 候选生成测试

- [ ] 相同种子产生相同的 7 轮基础候选和 3 轮高级候选。
- [ ] 每轮候选恰好 3 个且不重复。
- [ ] 两个等级不会混入同一轮。
- [ ] 不会产生无合法候选的轮次。
- [ ] 生成至少 10,000 组 Draft，统计每枚棋的出现频率。
- [ ] 任意棋子的出现率不得因实现错误显著异常。

### 验收标准

- 每方最终严格拥有 7 枚基础棋和 3 枚高级棋。
- Draft 不会出现全是高级棋或没有阵型主体的离谱阵容。

---

## 7. 第四阶段：逐枚实现新基础棋

每枚棋必须独立完成以下完整链条后，才能开始下一枚：

1. 将棋种加入类型和注册表。
2. 实现纯规则控制范围。
3. 明确 Fence、边缘和阻挡行为。
4. 增加中英文名称与说明。
5. 制作正式棋子图标。
6. 制作 5×5 或等价范围示意。
7. 接入 Draft 卡片。
8. 接入手牌和对手阵容摘要。
9. 接入棋盘棋子显示。
10. 接入“谁影响它 / 它影响谁”。
11. 接入规则书。
12. 增加规则单元测试。
13. 在桌面和手机完成手动验证。

### 基础棋验收

- 一句话可以说明能力。
- 通常控制约 4 格。
- 连续选择 3 枚不会自然形成必胜策略。
- 不依赖隐藏状态或复杂计数。
- 图标和范围图在小尺寸下可辨认。

---

## 8. 第五阶段：逐枚实现新高级棋

沿用基础棋的完整实现链条，并额外完成：

- [ ] 写出特殊规则的精确优先级。
- [ ] 验证与 Fence 的所有方向和边界情况。
- [ ] 验证多个同类高级棋同时存在时的关系。
- [ ] 验证 Collapse 移除棋子后，其范围是否正确重新计算。
- [ ] 验证清算复盘能够正确重现它的影响。
- [ ] 为容易误解的情况增加规则书示例。
- [ ] 确认它不是某枚基础棋的全面上位替代。

### 高级棋验收

- 玩家能从 Draft 卡片理解主要用途。
- 控制能力强，但存在位置、方向或阵型代价。
- 与堡垒、骑士和炮台相比拥有不同角色。
- 不会因为控制格数量过高而成为无条件必选。

---

## 9. 第六阶段：棋池组合检查

### 角色覆盖

分别检查两个棋池是否包含：

- 近距离支援。
- 远距离攻击。
- 分散控制。
- 集中控制。
- Fence 附近可用的棋。
- 开放区域可用的棋。
- 容易互保的棋。
- 适合拆阵的棋。

不是每个棋都需要独立角色，但候选不能经常出现三个用途完全相同的棋。

### 候选质量

从至少 100 个种子中人工抽查：

- 三个候选是否都可能有选择理由。
- 是否存在明显的永远不选棋。
- 是否存在一出现就必选的高级棋。
- 是否频繁出现三个功能近似的候选。
- 地图是否会让某一整类棋完全失效。

### 验收标准

- 大部分候选轮次都需要玩家结合地图和已有阵容判断。
- 选择不是简单比较“谁控制格更多”。

---

## 10. 第七阶段：平衡验证

AI 完整升级不在前期范围内，但规则稳定后必须建立可重复的基础模拟。

### 静态指标

每枚棋统计：

- 空棋盘中心平均控制格。
- 棋盘边缘平均控制格。
- 不同 Fence 密度下平均控制格。
- 实际能命中棋子的概率。
- 平均支援数与攻击数。
- 最终存活率。
- 被 Draft 选择率。

### 对局测试

- [ ] 每个新棋至少进行 20 局人工试玩观察。
- [ ] 记录出现该棋与未出现该棋的胜率差异。
- [ ] 记录每个高级棋的选择率。
- [ ] 长期接近 100% 选择率的高级棋必须重新评估。
- [ ] 长期接近 0% 选择率的棋必须明确其用途或调整。
- [ ] 优先调整棋子范围、限制或 Draft 出现率，不轻易改变 Collapse。

### 平衡原则

- 不要求所有棋在所有地图上同样强。
- 允许棋子在特定地形中特别优秀。
- 不允许某枚棋在几乎所有地图和阵容中都是最佳选择。
- 基础棋通过稳定性获得价值。
- 高级棋通过特殊上限获得价值，同时承担条件或位置成本。

---

## 11. 第八阶段：AI 接入

只有棋子规则和棋池基本稳定后才更新 AI。

### Draft AI

需要理解：

- 当前是基础阶段还是高级阶段。
- 自己已有的棋种和剩余复制上限。
- 当前地图 Fence 分布。
- 阵容是否缺少近程、远程、跳跃或互保能力。
- 新棋的标签和大致用途。

### 布阵 AI

需要做到：

- 枚举所有新棋的合法位置。
- 使用与玩家相同的控制范围函数。
- 正确处理 Fence。
- 正确评估新棋在完整 Collapse 中的结果。
- 不在 AI 代码中复制新棋规则。

### 验收标准

- AI 能合法完成包含全部新棋的 Draft 和布阵。
- 不出现不会使用某枚棋、库存遗留或非法落子的情况。
- AI 强弱调整不得反过来修改玩家规则。

---

## 12. 第九阶段：完整回归

### Classic

- [ ] 仍然只有原来的六种棋。
- [ ] 固定阵容数量不变。
- [ ] 新棋不会出现在手牌、规则书主要流程或 AI 库存中。
- [ ] 所有原有控制范围和 Collapse 测试通过。

### Skirmish

- [ ] 基础池和高级池完全分开。
- [ ] 每方得到 7 基础、3 高级。
- [ ] 新棋的中英文、图标、范围图和规则说明齐全。
- [ ] 新棋与 Fence、关系查看和复盘兼容。
- [ ] 手机 Draft 卡与手牌不会溢出。
- [ ] 相同种子可以复现地图与两阶段候选。

### 构建

- [ ] ESLint。
- [ ] 规则单元测试。
- [ ] Draft 大批量生成测试。
- [ ] 应用测试。
- [ ] 静态网页构建。
- [ ] Android 同步和真机测试。

---

## 13. 建议提交顺序

1. `refactor: add extensible piece registry`
2. `fix: isolate the classic fixed roster`
3. `feat: split core and advanced draft pools`
4. `test: validate tiered draft generation`
5. 每个新基础棋单独一个提交。
6. 每个新高级棋单独一个提交。
7. `docs: add bilingual piece rules`
8. `balance: tune draft weights and copy limits`
9. `feat: teach draft ai the expanded roster`
10. `test: run full piece and skirmish regression`

未经用户明确要求，不得自动提交或推送。

---

## 14. 本流程的完成定义

- [ ] 基础棋池至少有 5 种，目标为 6 种。
- [ ] 高级棋池至少有 5 种，目标为 6 种。
- [ ] 每枚棋都有完整双语、图标、范围图和精确规则。
- [ ] Classic 固定阵容不受新棋影响。
- [ ] Skirmish 先选 7 枚基础棋，再选 3 枚高级棋。
- [ ] 基础和高级不在同一候选轮混合。
- [ ] 所有棋都兼容 Fence、关系查看、Collapse 和复盘。
- [ ] 没有明显的永远必选或永远不选棋。
- [ ] 手机端能够清楚展示所有候选和动态手牌。
- [ ] AI 最终能够合法使用全部新棋。
- [ ] 全部自动检查和真机路径通过。

---

## 15. 内容设计的第一轮任务

UI 流程完成以后，内容流程第一轮只做：

1. 建立 PieceDefinition 与 `core / advanced` 字段。
2. 将剑兵、枪兵、射手归入基础池。
3. 将堡垒、骑士、炮台归入高级池。
4. 把 Classic 固定阵容与全局注册表分开。
5. 将 Draft UI 接入两个独立棋池，但暂时不新增棋。
6. 为新增棋建立统一设计卡模板。
7. 与用户讨论并批准首批新基础棋与新高级棋名单。

未经用户批准新棋纸面规则，不进入具体能力编码。

