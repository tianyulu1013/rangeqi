import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rangeqi.game',
  appName: 'Battle Array: Collapse',
  webDir: 'android-web'
};

export default config;
