# 《阵衡》发布工作流程

## 0. 目标与原则

本计划的目标，是把《阵衡》从已经具备完整核心玩法的网页游戏，推进为稳定、易学、可验证、可在 Android 商店发布的正式版本。

开发过程中遵守以下原则：

- 先保证规则正确，再增加表现与内容。
- 当前版本不新增棋种、不扩大棋盘、不增加额外资源系统。
- 每个阶段必须通过验收后，才能进入下一阶段。
- 网页版和 Android 版共用同一套游戏规则，不维护两套逻辑。
- 所有生成物、安装包、签名密钥与源代码分开管理。
- 修改已有功能时，必须补充相应测试，避免旧功能回归。

---

## 1. 整理当前工作区

### 目标

明确哪些改动属于正在开发的“清算复盘”，哪些属于 Android 工程，避免把未完成代码、构建缓存和正式源码混在同一次提交中。

### 任务

- [ ] 检查当前未提交文件：
  - `app/page.tsx`
  - `package.json`
  - `package-lock.json`
  - `capacitor.config.ts`
  - `android/`
- [ ] 确认本地改动全部属于《阵衡》，没有误混入其他项目文件。
- [ ] 更新 `.gitignore`，至少排除：
  - `android/**/build/`
  - Gradle 临时缓存
  - APK、AAB 等构建产物
  - 本地签名文件和签名配置
- [ ] 调整 ESLint 忽略范围，禁止扫描 Android 自动生成的 JavaScript 和构建文件。
- [ ] 把“复盘功能”和“Android 工程接入”拆成两个独立提交。
- [ ] 提交前再次确认 Git 作者身份和远端仓库。

### 验收标准

- `git status` 中不再出现 Android 构建缓存。
- `npm run lint` 只检查项目源码，不再产生数千条生成文件警告。
- 每个提交只包含一个明确主题，能够单独回退。

---

## 2. 修复当前阻塞错误

### 目标

恢复页面正常启动，并让现有构建与测试重新通过。

### 当前已知问题

- `displayPieces` 在声明前被 `stats` 引用，会触发运行时错误。
- `displayPendingIds` 和 `isReplaying` 已定义但尚未完整接入界面。
- 现有测试仍然检查已经删除的 `STRATEGY_DESCRIPTIONS`。
- 清算复盘只有部分状态和数据结构，没有完整操作入口。

### 任务

- [ ] 调整复盘派生状态的声明顺序：
  1. 先计算 `replayFrame`。
  2. 再计算 `displayPieces` 和 `displayPendingIds`。
  3. 最后基于 `displayPieces` 计算统计值、影响范围和棋盘映射。
- [ ] 检查所有 React Effect 的状态更新路径，避免同步连锁更新和重复渲染。
- [ ] 删除真正不再使用的变量，或把它们完整接入复盘显示。
- [ ] 更新过期测试，不再依赖已经删除的内部常量名。
- [ ] 确认中文和英文默认界面都可以正常首屏渲染。

### 验收标准

- 页面启动时没有白屏或控制台错误。
- `npm run lint` 通过。
- `npm test` 通过。
- `npm run build` 通过。
- 游戏可以正常开始、布满20枚棋子、进入清算并显示结果。

---

## 3. 完成清算复盘

### 目标

让玩家可以逐步回看阵型如何崩解，从而理解失败原因和学习策略。

### 数据设计

每个复盘帧应保存：

- 当前棋盘上的完整棋子列表。
- 当前被标记为即将离场的棋子 ID。
- 清算轮次。
- 阶段：初始、标记、移除、结束。
- 本步骤的中文或英文说明。

复盘帧必须是当时状态的独立快照，不能继续引用之后会被修改的数组或对象。

### 任务

- [ ] 在开始清算时保存“完整初始阵型”。
- [ ] 每轮计算后保存“最低生存值棋子已标记”。
- [ ] 同时移除棋子后保存“本轮移除完成”。
- [ ] 重新计算后进入下一轮。
- [ ] 清算结束时保存最终阵型。
- [ ] 在结果界面加入“复盘清算”按钮。
- [ ] 加入以下复盘控制：
  - 上一步
  - 下一步
  - 自动播放
  - 暂停
  - 退出复盘
- [ ] 显示当前步骤，例如“步骤 3/9”。
- [ ] 显示当前清算轮次和操作说明。
- [ ] 被标记棋子必须有明显但不刺眼的视觉状态。
- [ ] 复盘时禁止落子、撤回和重新触发清算。
- [ ] 退出复盘后恢复最终棋盘，而不是停留在中间状态。
- [ ] 切换语言时，复盘说明必须同步更新，或保存结构化事件后按当前语言重新生成文字。

### 必测场景

- [ ] 一轮清算后立即稳定。
- [ ] 多轮连续清算。
- [ ] 多枚棋子并列最低，同时离场。
- [ ] 炮架离场后，炮台范围改变并导致下一轮新死亡。
- [ ] 清算后双方均无棋子。
- [ ] 复盘过程中快速点击上一步和下一步。
- [ ] 自动播放到最后一步后自动停止。
- [ ] 退出复盘、再次进入复盘。

### 验收标准

- 玩家能够从第一帧一直查看到最终帧。
- 手动步进和自动播放显示相同结果。
- 每一帧显示的攻击、支援、生存值与该帧棋盘一致。
- 复盘不改变真实最终结果。

---

## 4. 拆分并测试规则引擎

### 目标

把最重要的规则逻辑从大型页面组件中分离出来，使其可以独立测试、模拟和复用。

### 建议模块

- `src/core/types.ts`
  - 玩家、棋种、棋子、阶段、统计值等类型。
- `src/core/pieces.ts`
  - 六类棋子的数量、攻击范围和显示信息。
- `src/core/rules.ts`
  - 合法落子、控制范围、攻击、支援、生存值。
- `src/core/settlement.ts`
  - 单轮清算、完整清算、复盘帧生成。
- `src/core/scoring.ts`
  - 存活棋子、控制格与胜负判断。
- `src/core/ai.ts`
  - AI候选生成、评分和不同策略权重。

页面组件只负责显示和用户交互，不自行实现规则。

### 规则测试

#### 棋子范围

- [ ] 剑兵控制上下左右相邻4格。
- [ ] 枪兵控制斜向相邻4格。
- [ ] 射手控制上下左右正好相距2格的位置。
- [ ] 骑士控制“日”字8个位置。
- [ ] 堡垒控制周围8格。
- [ ] 所有棋子在边缘和角落不会越界。

#### 炮台规则

- [ ] 没有炮架时不控制该方向。
- [ ] 越过第一枚棋子后开始控制。
- [ ] 控制范围包括遇到的下一枚棋子。
- [ ] 遇到下一枚棋子后立即停止。
- [ ] 炮架和目标不区分敌我，几何规则保持一致。
- [ ] 炮架被清除后，炮台范围会重新计算。

#### 攻击与支援

- [ ] 同阵营范围计为支援。
- [ ] 异阵营范围计为攻击。
- [ ] 生存值严格等于支援数减攻击数。
- [ ] 一个格子被多枚棋子控制时分别计数。
- [ ] 棋子不对自己产生支援。

#### 连锁清算

- [ ] 只移除全场生存值最低且小于0的棋子。
- [ ] 最低值并列时同时移除。
- [ ] 每轮移除后重新计算全场。
- [ ] 所有棋子生存值不低于0时停止。
- [ ] 同一局面每次计算结果一致。
- [ ] 清算不会修改传入的原始棋盘数据。

#### 胜负判断

- [ ] 存活棋子更多者获胜。
- [ ] 存活数相同则比较控制格。
- [ ] 存活数和控制格均相同则平局。

### 验收标准

- 核心规则模块不依赖 React 或浏览器 DOM。
- 所有规则测试通过。
- 网页版和 Android 版调用同一个规则模块。
- 对已有规则的修改必须同时修改测试。

---

## 5. 建立AI模拟与平衡报告

### 目标

用批量数据判断先后手、棋种和AI策略是否失衡，避免只凭少量人工对局调整规则。

### 任务

- [ ] 为AI提供固定随机种子，确保问题可以复现。
- [ ] 建立无需浏览器界面的模拟脚本。
- [ ] 四种AI策略互相对战，每组至少运行1000局：
  - 均衡
  - 猛攻
  - 结阵
  - 控场
- [ ] 交换红蓝和先后手后重复模拟。
- [ ] 记录：
  - 胜率
  - 平局率
  - 平均存活棋子数
  - 控制格决胜比例
  - 每种棋子的平均放置顺序
  - 每种棋子的最终存活率
  - 每局清算轮数
  - 各策略最常见开局位置
- [ ] 查找固定必胜开局或高度重复套路。
- [ ] 检查堡垒、炮台是否具有压倒性价值。
- [ ] 检查随机AI是否真正按局随机，而不是每步随机切换人格。

### 调整原则

- 优先调整AI评分和信息展示。
- 只有数据持续证明规则失衡，才调整棋子数量或范围。
- 一次只调整一个变量，调整后重新跑完整模拟。
- 不以“所有AI胜率完全相同”为目标；不同风格可以有强弱，但不能存在明显无解套路。

### 验收标准

- 先手优势处于可接受范围。
- 红蓝交换后不存在阵营逻辑偏差。
- 四种AI有可感知差异，但都能合法完成对局。
- 没有单一固定开局长期统治模拟结果。
- 生成一份可保存的平衡报告。

---

## 6. 制作一分钟互动教学

### 目标

让第一次接触《阵衡》的玩家不读完整规则书，也能理解攻击、支援、生存值与连锁清算。

### 教学流程

#### 步骤1：认识控制范围

- 指定玩家选择剑兵。
- 高亮上下左右四个控制格。
- 玩家完成一次指定落子。

#### 步骤2：认识支援

- 引导玩家把友方棋子放入剑兵控制范围。
- 显示“支援 +1”。
- 在棋子旁展示当前生存值变化。

#### 步骤3：认识攻击

- 放置或引导放置一枚敌棋进入范围。
- 显示“受到攻击 +1”。
- 强调生存值公式：支援－攻击。

#### 步骤4：认识危险状态

- 让一枚棋子的生存值变为负数。
- 明确标记为“濒死”，但暂不立即移除。
- 说明所有棋子放完后才统一清算。

#### 步骤5：认识连锁清算

- 使用一个预设小阵型。
- 第一轮移除一枚棋子。
- 展示支援关系消失。
- 第二枚棋子因此变成负数并在下一轮离场。

### 体验要求

- [ ] 首次启动显示“快速教学”和“直接对局”。
- [ ] 教学可以随时跳过。
- [ ] 完成一次后不再强制弹出。
- [ ] 设置页可以重新进入教学。
- [ ] 中文和英文教学均完整。
- [ ] 教学不记录为正式战绩。

### 验收标准

- 未读规则的测试玩家可以独立完成一局。
- 玩家能说出“生存值＝支援－攻击”。
- 玩家能理解棋子不会立即死亡，而是在最后逐轮清算。

---

## 7. 移动端体验验收

### 目标

保证小屏手机和平板上都能准确落子、理解局面和完成一局。

### 设备范围

- 小屏 Android 手机。
- 主流比例 Android 手机。
- 大屏 Android 手机。
- Android 平板。
- 系统字体放大模式。
- 中文和英文界面。

### 检查项目

- [ ] 7×7棋盘每格仍可准确点击。
- [ ] 棋子图标在小尺寸下可区分。
- [ ] 红蓝之外还有形状或纹理辅助，满足辨色需求。
- [ ] 设置、规则书、结果页和复盘没有超出屏幕。
- [ ] 刘海、状态栏和底部手势区域留出安全距离。
- [ ] 横竖屏策略明确；若只支持竖屏，应在原生配置中锁定。
- [ ] 返回键优先关闭弹窗，而不是直接退出游戏。
- [ ] 游戏过程中切到后台再回来不会丢失或重置。
- [ ] 快速连续点击不会重复落子。
- [ ] AI思考期间操作明确禁用。
- [ ] 清算动画期间不能触发冲突操作。

### 建议增加

- [ ] 轻量音效开关。
- [ ] 轻触震动开关。
- [ ] 保存设置偏好。
- [ ] 保存当前未完成对局，进程被系统杀死后可以继续。

### 验收标准

- 每类目标设备至少完整完成3局。
- 无法复现误触、双落子、卡死、白屏和无法关闭弹窗。
- 一局从启动到结果页不依赖网络。

---

## 8. 整理 Android 正式工程

### 目标

从现有 debug APK 过渡到可上传商店的正式 AAB。

### 当前状态

- Capacitor Android 工程已经生成。
- 应用ID为 `com.rangeqi.game`。
- 当前版本为 `versionCode 1`、`versionName 1.0`。
- 已生成过 debug APK。
- 当前图标和启动页仍是 Capacitor 默认图案。
- 尚未配置正式签名和正式 AAB 流程。

### 任务

- [ ] 确认最终应用ID，发布后不再随意更换。
- [ ] 确认商店显示名称“阵衡”和英文名称。
- [ ] 配置固定的版本号管理方法。
- [ ] 创建正式上传密钥，并在安全位置备份。
- [ ] 密钥和密码只通过本地安全配置读取，不进入 Git。
- [ ] 配置 release signing。
- [ ] 生成 release AAB。
- [ ] 使用 Android Studio 或命令行验证签名。
- [ ] 检查最终 Manifest 权限，仅保留必要权限。
- [ ] 确认应用完全离线时是否仍需要 INTERNET 权限。
- [ ] 生成并安装 release APK，在真机进行最终冒烟测试。

### 验收标准

- release AAB成功生成。
- 正式包使用预期应用ID、版本号、图标和名称。
- release包可以安装并完整完成对局。
- Git中不存在密钥、密码或构建产物。

---

## 9. 品牌图标与启动页

### 目标

用《阵衡》自己的视觉标识替换当前 Capacitor 默认蓝色图标。

### 任务

- [ ] 以“衡”字或阵型平衡关系为核心设计应用图标。
- [ ] 确保图标在48px小尺寸下仍可辨认。
- [ ] 准备 Android Adaptive Icon：
  - 前景层
  - 背景色或背景层
  - 圆形裁切预览
  - 方圆角裁切预览
- [ ] 准备启动页，保持简洁，不放规则和小字。
- [ ] 替换所有密度目录中的启动图和图标资源。
- [ ] 检查浅色、深色桌面背景下的辨识度。

### 验收标准

- 应用列表、桌面、设置页和启动页均不再显示 Capacitor默认图案。
- 图标与游戏内“衡”字品牌一致。
- 不同Android启动器裁切后主体均完整。

---

## 10. 商店合规与素材

### 目标

准备完整、真实且与正式包一致的商店提交材料。

### 合规材料

- [ ] 隐私政策页面。
- [ ] 数据安全声明。
- [ ] 内容分级问卷所需答案。
- [ ] 广告声明：如无广告应保持代码与声明一致。
- [ ] 账户声明：如无账户，应确保产品内没有无效登录入口。
- [ ] 权限说明：每项权限都能说明用途。
- [ ] 联系邮箱和支持页面。

### 商店文案

- [ ] 中文应用名和英文应用名。
- [ ] 一句话短描述。
- [ ] 完整描述。
- [ ] 六类棋子的简要介绍。
- [ ] 强调“静态布阵、攻击与支援、连锁清算”。
- [ ] 避免把AI描述成在线或生成式AI；它是本地策略程序。

### 商店图片

- [ ] 512×512应用图标。
- [ ] Feature Graphic。
- [ ] 手机截图。
- [ ] 平板截图。
- [ ] 中文截图。
- [ ] 英文截图。
- [ ] 截图至少覆盖：
  - 正常布阵
  - 范围预览
  - 生存值检查
  - 连锁清算
  - 最终结果或复盘

### 验收标准

- 商店文案描述的功能在正式包中全部存在。
- 截图来自正式 release版本，不使用与实际界面不一致的概念图。
- 隐私政策网址可公开访问。

---

## 11. 发布前最终检查

### 自动检查

- [ ] `npm run lint`
- [ ] `npm test`
- [ ] `npm run build`
- [ ] `npm run build:static`
- [ ] Android release AAB构建
- [ ] Git工作区干净

### 人工冒烟测试

- [ ] 玩家执红，完成一局。
- [ ] 玩家执蓝，完成一局。
- [ ] 随机先手完成多局。
- [ ] 四种AI风格各完成一局。
- [ ] 本地双人模式完成一局。
- [ ] 中文模式完成一局。
- [ ] 英文模式完成一局。
- [ ] 三种配色和三种棋子显示方式均可用。
- [ ] 撤回、重新开始、设置和规则书正常。
- [ ] 清算复盘完整可用。
- [ ] 无网络环境可以启动并完成游戏。
- [ ] 后台恢复正常。

### 发布档案

- [ ] 记录Git提交号。
- [ ] 记录versionCode和versionName。
- [ ] 保存AAB的校验值。
- [ ] 编写版本发布说明。
- [ ] 给正式提交打Git标签。
- [ ] 在内部测试轨道安装商店分发版本，再做一次最终测试。

### 验收标准

- 自动检查全部通过。
- 正式商店包与测试包为同一个构建来源。
- 没有未提交的发布相关代码。
- 所有商店材料和应用实际表现一致。

---

## 12. 建议里程碑

### 里程碑 A：恢复稳定开发版

- 当前页面能够启动。
- 清算复盘完成。
- lint、test、build全部通过。

### 里程碑 B：规则可信

- 规则引擎完成拆分。
- 核心规则测试完整。
- AI模拟报告完成。
- 先后手和平衡问题已有数据结论。

### 里程碑 C：新手可独立游玩

- 一分钟互动教学完成。
- 手机端体验通过。
- 至少进行一轮外部试玩并记录反馈。

### 里程碑 D：Android 内部测试

- 正式图标和启动页完成。
- release签名与AAB完成。
- 隐私政策、商店文案和截图完成。
- 上传内部测试轨道。

### 里程碑 E：提交审核

- 商店分发包真机验证通过。
- 发布清单全部打勾。
- 正式提交商店审核。

---

## 当前建议从这里开始

下一次实际开发建议只处理“里程碑 A”，不要同时制作商店图、调整AI权重或新增规则。

具体顺序：

1. 修复 `displayPieces` 声明顺序导致的启动错误。
2. 完成复盘入口和控制按钮。
3. 修复复盘高亮、统计和退出状态。
4. 更新已经过期的测试。
5. 清理 lint 范围和 Android 构建缓存。
6. 确认 `npm run lint`、`npm test`、`npm run build` 全部通过。
7. 将复盘和 Android 工程分别提交。

完成以上步骤后，再进入规则引擎拆分和系统测试阶段。
